# 🚀 OpenClaw Workspace Bootstrap

## Purpose

This document defines how the OpenClaw workspace initializes,
what must exist before startup, and how the environment is prepared.

---

# 1️⃣ Required Services

Before starting Control UI:

- Gateway must be running
- Valid Gateway Token configured
- Workspace directory accessible

Start gateway:

```bash
openclaw gateway
